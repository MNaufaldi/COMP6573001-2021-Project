const { addType, updateType, deleteType } = require('./TypeMutations');
const { addFood, updateFood, deleteFood} = require('./FoodMutations')

module.exports = {
    addType,
    updateType,
    deleteType,
    addFood,
    updateFood,
    deleteFood
}